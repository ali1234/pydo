from pydo import *

from .. import b

this_module().dependencies = [b]
this_module().enabled = False


def check():
    print(this_module().friendly_name, 'module will never be built because it overrides check.')
    return False